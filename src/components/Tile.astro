---
/*
 * One item on the shelf. Two sizes: `feature` is the single loud thing at
 * the top of the home page, `compact` is a row member. Colour comes from
 * the collection, never from the index, so it means something and does
 * not change when a new experiment lands. Planned items are not links.
 */
import { collectionOf } from "../lib/collections";
import type { ShelfItem } from "../lib/shelf";
import TilePreview from "./TilePreview.astro";

interface Props {
	item: ShelfItem;
	size?: "feature" | "compact";
	index?: number;
}

const { item, size = "compact", index = 0 } = Astro.props;
const collection = collectionOf(item.collection);
const Tag = item.planned ? "div" : "a";
---

<Tag
  href={item.planned ? undefined : item.href}
  class:list={["tile bob-in", `tile-${size}`, { planned: item.planned }]}
  style={`--i: ${index}; --accent-colour: ${collection.colour};`}
>
  <div class="preview-wrap">
    <TilePreview slug={item.slug} title={item.title} />
  </div>
  <div class="text">
    <span class="title">
      {item.title}
      {item.planned && <span class="tag">soon</span>}
      {item.isNew && <span class="tag tag-new">new</span>}
    </span>
    <span class="summary">{item.summary}</span>
    {size === "feature" && <span class="cue">Open it →</span>}
  </div>
</Tag>

<style>
  .tile {
    display: flex;
    flex-direction: column;
    gap: 14px;
    padding: 18px;
    border: 3px solid var(--line);
    border-radius: 20px;
    background: var(--surface);
    color: var(--ink);
    box-shadow: 5px 5px 0 var(--line);
    text-decoration: none;
    transition:
      translate var(--duration-fast) var(--ease-out),
      box-shadow var(--duration-fast) var(--ease-out);
  }
  a.tile:hover {
    translate: -3px -3px;
    box-shadow: 8px 8px 0 var(--line);
  }
  .tile.planned {
    border-style: dashed;
    box-shadow: none;
    opacity: 0.75;
  }

  /* The collection colour, as a band above the drawing. */
  .preview-wrap {
    position: relative;
    border-radius: 14px;
  }
  .preview-wrap::before {
    content: "";
    position: absolute;
    inset: -3px -3px auto;
    height: 8px;
    border: 3px solid var(--line);
    border-bottom: 0;
    border-radius: 12px 12px 0 0;
    background: var(--accent-colour);
    z-index: 1;
  }
  .preview-wrap :global(.preview) {
    border-top-left-radius: 4px;
    border-top-right-radius: 4px;
  }

  .text {
    display: flex;
    flex-direction: column;
    gap: 5px;
  }
  .title {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 9px;
    font-size: 21px;
    font-weight: 800;
    letter-spacing: -0.03em;
    line-height: 1.05;
  }
  .summary {
    font-size: 13.5px;
    font-weight: 500;
    line-height: 1.45;
    color: var(--ink-muted);
  }
  .tag {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.04em;
    border: 2px solid currentColor;
    border-radius: 6px;
    padding: 2px 6px;
  }
  .tag-new {
    background: var(--yellow);
    color: var(--accent-ink);
    border-color: var(--accent-ink);
  }

  /* The one loud thing. */
  .tile-feature {
    gap: 22px;
    padding: 22px;
    border-radius: 26px;
    box-shadow: 8px 8px 0 var(--line);
  }
  .tile-feature .title {
    font-size: clamp(30px, 4vw, 44px);
  }
  .tile-feature .summary {
    font-size: 16px;
    max-width: 52ch;
    color: var(--ink-soft);
  }
  .tile-feature .cue {
    margin-top: 8px;
    font-family: var(--font-mono);
    font-size: 13px;
    font-weight: 500;
  }
  .tile-feature .preview-wrap :global(.preview) {
    height: 210px;
  }
  a.tile-feature:hover {
    translate: -4px -4px;
    box-shadow: 12px 12px 0 var(--line);
  }
  @media (min-width: 900px) {
    .tile-feature {
      display: grid;
      grid-template-columns: minmax(0, 1.1fr) minmax(0, 1fr);
      align-items: center;
      gap: 32px;
    }
    .tile-feature .preview-wrap :global(.preview) {
      height: 260px;
    }
    .tile-feature .text {
      align-self: center;
    }
  }
</style>
